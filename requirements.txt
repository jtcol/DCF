# Pinned for stable, prebuilt wheels on Python 3.13 (avoid bleeding-edge 3.14 segfaults).
# Compiled libraries are pinned exactly; pure-Python deps use floors.
streamlit==1.59.1
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0
lxml==5.3.0
plotly>=5.24
yfinance>=0.2.50
requests>=2.32
python-dotenv>=1.0
